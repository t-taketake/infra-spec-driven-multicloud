name: GCP Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'infra/gcp/**'
      - 'modules/gcp/**'
  pull_request:
    branches:
      - main
    paths:
      - 'infra/gcp/**'
      - 'modules/gcp/**'

permissions:
  id-token: write
  contents: read

jobs:
  terraform-plan:
    name: Terraform Plan (GCP)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud (OIDC)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.0

      - name: Terraform Init & Plan
        run: |
          cd infra/gcp/vpc
          terraform init
          terraform plan -out=tfplan.binary
          terraform show -json tfplan.binary > plan.json

      - name: Setup Infracost
        uses: infracost/actions/setup@v3
        with:
          api-key: ${{ secrets.INFRACOST_API_KEY }}

      - name: Generate Infracost JSON
        run: |
          cd infra/gcp/vpc
          infracost breakdown --path plan.json --format json --out-file infracost.json

      - name: Post Infracost Comment
        uses: infracost/actions/comment@v1
        with:
          path: infra/gcp/vpc/infracost.json
          behavior: update

  deploy:
    name: Deploy to GCP
    runs-on: ubuntu-latest
    needs: terraform-plan
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud (OIDC)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Setup Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker us-central1-docker.pkg.dev

      - name: Build and Push to Artifact Registry
        run: |
          # docker build -t us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/app-repo/app:${{ github.sha }} .
          # docker push us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/app-repo/app:${{ github.sha }}
          echo "Build and push step (commented for now)"

      - name: Deploy to Cloud Run
        run: |
          # gcloud run deploy my-service --image us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/app-repo/app:${{ github.sha }} --region us-central1
          echo "Cloud Run deployment step (commented for now)"

      - name: Purge CDN Cache
        run: |
          # gcloud compute url-maps invalidate-cdn-cache my-url-map --path "/*"
          echo "CDN purge step (commented for now)"
